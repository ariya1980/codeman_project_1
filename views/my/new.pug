extends ../layout

block title
    = `Beverage - Add new category`

block content
    h1.title Add Category
        form(action="/beverages/categories",method="POST")
            .field
                .control
                    input.input(name="name")
            .field
                .control
                    button.button.is-link.is-fullwith Add