extends ../layout

block title
    = `Beverage Application - Login`

block content
    h1.title Beverage Login
        form(action="/beverages/users/session",method="POST")
            .field
                .control
                    input.input(name="username")
            .field
                .control
                    input.input(name="password")
            .field
                .control
                    button.button.is-link.is-fullwith login